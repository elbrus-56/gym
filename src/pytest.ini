[pytest]
asyncio_mode = auto
# Устанавливаем область видимости цикла событий для асинхронных фикстур явно
# чтобы избежать DeprecationWarning и подготовиться к будущим изменениям.
# 'function' - рекомендуемый скоуп по умолчанию в будущем.
asyncio_default_fixture_loop_scope = function 